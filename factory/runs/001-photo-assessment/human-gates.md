# Human gates — photo-assessment

A human gate is a point where the line stops and a person decides. The policy is in
`factory/handoff-map.yaml` under `human_gate_policy`.

**Statuses:** `recorded-open` (fired, decision still open, line continued on a stated assumption) ·
`hard-stop` (line cannot continue) · `paused-approved` · `paused-blocked` · `missed` (should have
fired and did not — a finding, same weight as a broken seam) · `n/a` · `closed` (the owner answered
it and the answer is in a file a role can read).

`closed` is not in the plugin's template. It was added on this run because there was no way to say
"answered", which is the most common thing that happens to a gate. That is a change to make in the
`ai-factory` repo, not here — recorded in `run-record.md`.

**A decision that exists only in this chat has not been made.** When you answer a gate, write the
answer into a file a role can read, then name that file below. Otherwise the next role is running on
hand-fed context and the run is no longer honest.

| # | Slot | What triggered it | Status | Decision, and who made it | Written into which file, read next by which role | Assumption the line continued on |
| - | ---- | ----------------- | ------ | ------------------------- | ------------------------------------------------ | -------------------------------- |
| 1 | 100-consulting | **No retention rule for the plant photo.** `factory/feature.md` requires storage "with a retention rule" and treats the photo as personal data. Neither input file gives a period. Policy match: "a use case touches personal data and no retention rule exists" | `closed` 2026-08-24 — the hard stop at 400-architecture is lifted | **180 days, then automatic deletion**, by the owner. A storage lifecycle rule deletes the file, not application code. The text result is kept longer. A user can delete their photos on demand. The owner first asked for research; the research says GDPR Article 5(1)(e) names no period for anything and requires the owner to choose one, justify it, and actually delete | `factory/feature.md`, "Human decisions already made". Read next by 400-architecture, then 900-security | 100 continued because its four outputs do not need the number. The brief still shows O-5 as open, which is correct for what 100 knew when it ran |
| 2 | 100-consulting | **The AWS six-month end date cannot be sourced.** `factory/feature.md` says "Write the six-month end date into the context brief". Neither input records the date the AWS account was opened. Policy match: "the brief needs a number nobody can source" | `closed` 2026-08-24 — the hard stop at 800-infra is lifted | **Account opened 2026-07-01, so the free window ends 2026-12-31**, by the owner. That is four months from today, not six | `factory/feature.md`, "Known constraints the line must respect". Read next by 800-infra | The brief carries the rule and an empty date. Nothing downstream was invented from it |
| 3 | 100-consulting | **The value hypothesis needs a business number.** "8 accepted actions out of 10" and "6 assessments in 8 weeks" are proposals by this role, not measurements. Policy match: "a subagent needs a policy, compliance, budget or release decision" — what a business number means is the owner's | `closed` 2026-08-24 | **Keep both numbers, marked provisional**, by the owner. They stay until the first 20 real assessments give a measurement to replace them with. A hypothesis with no number cannot be tested, and a wrong number that is labelled wrong is still better than none | `00-context-brief.md` §3 and O-1 in §7 stay as written. Read next by 600 QA when it writes the AI evaluation | Nothing changed. The numbers the line already used are the ones the owner kept |
| 4 | 100-consulting | **Which languages ship is not decided.** Both inputs say "more than one language" and neither names one. Naming one would be choosing scope. Policy match: "a subagent chooses scope beyond `factory/feature.md`" | `closed` 2026-08-24 | **Hungarian and English in run 1**, by the owner | `factory/feature.md`, "Human decisions already made". Read next by 200-product, then 300-design and 500-engineering | The line continued with "more than one language" unresolved. 300 Design and 500 Engineering need the answer before they fix any text |
| 5 | 100-consulting | **Whether the EU AI Act applies to this project is a legal question.** Article 50 transparency duties became applicable on 2 August 2026. Policy match: "anything with a legal or privacy consequence" | `closed` 2026-08-25 | **Show the notice, take no legal advice, for run 1**, by the owner. Every screen showing an assessment says it came from an AI model. Whether the Act applies is left unanswered on purpose, because the answer changes nothing while the app has one user and is not offered to anyone. **Scoped to run 1: this re-opens the day the app is offered to another person**, and that is a different question | `factory/feature.md`, "Human decisions already made". Read next by 300-design, which draws the notice, then 900 Security. `00-context-brief.md` §5.4 still shows it open, written before the answer | The line continued on the cheap safe assumption, and the owner kept it |
| 6 | 100-consulting | **`03-market.md` W-4 proposes a second outside API (`plant.health` by Kindwise) that the model path would call.** Policy match: "a subagent proposes a new tool the model may call" | `closed` 2026-08-25 | **Rejected outright, not deferred**, by the owner: *"I won't accept it, I'm not interested about Kindwise."* This is stronger than the market scan's "worth considering later". No role may propose a second opinion service again, and no design should leave a place for one. The reasons already in the file stand: double cost per assessment, a second outside service to keep working, and a rule needed for what the screen shows when two services disagree | `factory/feature.md`, "Human decisions already made". Read next by 300-design and 400-architecture. **`03-market.md` §2 W-4 and `00-prd.md` §6.2 still read "worth considering later"** — both were written before the answer. `factory/feature.md` is newer and wins, per finding 14 | Nothing was built on it either way |
| 7 | 100-consulting | Spending money, deploying anything, accepting a risk, reversing an ADR | `n/a` | This role proposed no spend, no deploy and no risk acceptance. The cost table in `00-context-brief.md` §4 is priced research, not a purchase | — | — |
| 8 | run input (seam 0c) | **"Confidence" was never defined**, and it decides what the user sees when the model is unsure. That is close to "what gets built" | `closed` 2026-08-24, **answered by the assistant on the owner's instruction** | Confidence is a band, not a number: `likely` · `unsure` · `cannot-tell`. No percentage on screen. Two agreement bars are provisional. The owner read the material folder question and told the assistant to answer it. **Recorded here because the assistant decided it, not the owner** — if it turns out wrong, this row is where to look | `factory/feature.md`, section "What confidence means on this project". Read next by 200-product, then 300-design, 500-engineering and 600-qa | — |
| 9 | run input (finding 6) | **Which account pays for a model call.** It decides whether a cost guardrail can be an AWS alarm at all | `closed` 2026-08-24 | **Two bills.** The Anthropic API is paid from the owner's own Anthropic credits. Bedrock is not used. So no AWS budget alarm can see a model call, and a runaway loop burns credits rather than closing the AWS account | `factory/feature.md`, "Human decisions already made". Read next by 800-infra, then 900-security | The brief left it unverified and said so. Nothing downstream was invented from it |
| 10 | 100-consulting, second pass (seam 0f) | **How long the assessment *text* is kept.** `factory/feature.md` closes the photo question with 180 days and says the text result is kept "longer", with no period. The text is still personal data — it is a record about a named user's home. Policy match: "a use case touches personal data and no retention rule exists" | `closed` 2026-08-24 — the hard stop is lifted. **This was the last hard stop on the run** | **No clock. The text lives as long as the pot does**, by the owner. Delete the pot and its assessments go; delete the account and everything goes. The answer changed the shape of the field rather than filling it in, the same way gate 8 did: a fixed period would delete the early part of a plant's history, which is the part worth having, and GDPR Article 5(1)(e) asks for no longer than the purpose needs. Backstop: **an account with no sign-in for 12 months is deleted, warned by email at 11 months.** The owner chose 12 over 24, holding less data at the cost of a shorter grace period | `factory/feature.md`, "Human decisions already made". Read next by 400-architecture, which designs the data lifecycle, then 900-security. **`00-context-brief.md` §5.4, O-10 in §7 and `02-decisions.md` D-12 still say it is open** — they were written before the answer and no role has re-read them. `factory/feature.md` is the newer file and wins, per finding 14 | The second pass continued because none of 100's four outputs needs the number. UC-5's Viability gate in `01-use-cases.md` passes on the photo rule and carries this as its one condition. It becomes a hard stop when 400 Architecture designs the data lifecycle |
| 11 | 200-product (G-1) | **The model response grew a fourth field.** `factory/feature.md` names three: verdict, confidence, next action. UC-2's Viability condition needs the follow-up date in a shape the code can read. Without it US-03, the care task, cannot exist. Policy match: "a subagent chooses scope beyond `factory/feature.md`" — this adds to the input rather than filling a blank in it | `closed` 2026-08-24 | **Four fields**, by the owner. The fourth is the follow-up in whole days, chosen by the model, because how soon you check a plant again depends on how bad it looks. The rejected option was a fixed table from verdict to interval — cheaper and fully testable, but it gives the same date for the same verdict every time. 200 had already written every story this way, so nothing it wrote needs changing | `factory/feature.md`, "Human decisions already made". Also in `00-prd.md` §5.3 and US-02. Read next by 300-design, then 400-architecture and 500-engineering, which puts it in the contract | Four fields. Everything 200 wrote about the care task rests on it, and the answer matched |
| 12 | 200-product (G-2) | **The per-user daily limit on assessments.** `00-context-brief.md` §4 works out that an unprotected endpoint empties the whole credit balance in about a minute, and no input names the limit that stops it. Policy match: spending money, which `CLAUDE.md` lists as never the model's | `closed` 2026-08-24 — the hard stop is lifted | **10 assessments per user per day**, by the owner. The check runs before the model call and before any retry, so a failed call still counts. Expected real use is about 30 a month, so this is ten times headroom. 5 a day was rejected as too tight for an evening spent checking several pots; 20 was rejected because the cap stops protecting much | `factory/feature.md`, "Human decisions already made". Read next by 400-architecture, which decides where it is enforced, then 800-infra and 900-security | **None was needed.** 200 refused to propose a number here, unlike the four below. That was the right call: every other blank on this run costs a re-write, this one costs money |
| 13 | 200-product (G-5) | **The time budget from first tap to result.** UC-1 says the flow must be "short enough to finish while standing" and hands the number to 300 Design. An acceptance criterion cannot wait for a later role | `closed` 2026-08-25 | **30 seconds**, by the owner — 200's proposal, kept. Measured from the tap to something on screen, so it covers the resize, the upload and the model call. 15 seconds was rejected because a weak signal can spend that on the upload alone, so the app would throw away answers it had already paid for. 60 seconds was rejected because a minute in front of a plant reads as broken, and the user taps again, which costs a second call | `factory/feature.md`, "Human decisions already made". Read next by 300-design, which decides what the wait looks like, then 400-architecture, which has to fit its timed flow inside it | 30 seconds, and the answer matched |
| 14 | 200-product (G-6) | **The list of ten verdict codes.** UC-1 ends with "what the list of possible verdicts is — 200 Product decides", so the routing is correct and the role did its job. But it built the list from symptom words in its three input files, with no plant reference. A missing code costs a plant | `closed` 2026-08-25, **with a review date** | **Ship the ten as written, review after the first 20 real assessments**, by the owner. Accepted without a plant reference because the list cannot fail silently: `other` and `nothing-wrong` mean the model is never forced to invent a fault, so a missing code shows up as a pile of `other` answers, which is countable. The 40-photo test set exposes the rest. Checking it against a plant reference first was rejected as a new input to the whole line rather than a fix to one list | `factory/feature.md`, "Human decisions already made". `00-prd.md` §5.2 holds the codes. Read next by 300-design, 500-engineering and 600-qa. **600 QA owns the review**: a rising share of `other` is the signal | The ten codes as written, and the answer matched |
| 15 | 200-product (G-7) | **How long a session lasts for a returning user.** UC-4 says "long enough" and names no period | `closed` 2026-08-25 | **30 days**, by the owner — 200's proposal, kept. 7 days was rejected because the app is opened every few weeks, not daily, so the user would meet the sign-in screen almost every visit. 90 days was rejected because a lost phone would stay signed in to photos of the inside of a home for three months | `factory/feature.md`, "Human decisions already made". Read next by 400-architecture, which decides how sign-in works and where tokens live | 30 days, and the answer matched |
| 16 | 200-product (G-8) | **How fast the kill-switch must take effect.** UC-7 requires it to work without a deploy and gives no time. `CLAUDE.md` lists owning the kill-switch as never the model's | `closed` 2026-08-25 | **60 seconds**, by the owner — 200's proposal, kept. The on/off value may be cached for up to a minute, so a few calls can slip through after it is flipped. Checking on every request was rejected as the more complicated answer: with no cached value, someone must decide what happens when that read itself fails, on or off. 5 minutes was rejected because the switch exists for when something is already going wrong | `factory/feature.md`, "Human decisions already made". Read next by 400-architecture, then 800-infra, which builds it | 60 seconds, and the answer matched |
| 17 | 200-product (G-9) | **The retry limit after a failed model call.** UC-6 hands it to 800 Infra with no number. Every retry is a paid call, so this is gate 12 in a second form | `closed` 2026-08-25, **reversed 2026-08-26** | **Reversed. One attempt, no retry**, by the owner on 2026-08-26, after the pre-mortem showed the retry broke NFR-10 and squeezed the time budget. The original answer follows, because the reasoning in it is still the reasoning for run 3. Originally: **Two attempts total: one try, one retry**, by the owner. Backoff between them, both inside the 30 seconds of gate 13. It retries only a timeout, a 429 or a 503 — never a bad request, a rejected photo, or an empty credit balance. Both attempts count against the 10-a-day limit. The owner first proposed **five tries with exponential backoff**, which is the standard answer and is wrong here for two reasons worth keeping: every attempt is a paid model call, and 1+2+4+8 seconds of backoff alone overruns the 30-second limit before the fifth call is even made. Five tries becomes right once the assessment runs in the background — backbone 6, run 3 | `factory/feature.md`, "Human decisions already made". Read next by 400-architecture, then 800-infra and 500-engineering, which writes the failure messages | The rule without the number. The number now exists |
| 18 | the owner, reading `00-prd.md` (finding 15) | **There is no cap on total spend, only on calls per user per day.** 10 a day for a month is 300 calls: $6.00 on Opus 5, $1.20 on Haiku 4.5. So the daily limit stops a script but does not keep spend inside the credit balance. Policy match: spending money, and accepting a risk | `closed` 2026-08-25 | **No cap. The credit runs out and the feature stops**, by the owner. The balance reaching zero is the stop, so a cap would be a second number to pick, enforce and keep correct that could only ever fire before the one that already works. The owner accepts that the feature can go dark with no warning, because topping up is manual anyway and nothing else in the product stops | `factory/feature.md`, "Human decisions already made". Read next by 400-architecture, then 500-engineering and 800-infra, which must treat an empty balance as a named failure state and must **not** retry it | The line continued on the daily limit alone. That was enough against a script, and the owner has now said it does not need to be more |
| 19 | the owner, before 300-design ran | **The visual identity.** `factory/subagent-slots/300-design.md` hands brand voice and visual identity back to a person and never decides them. Nothing in any input named a colour, a typeface or a look, so the role would have stopped — or worse, picked one quietly. Policy match: "a subagent chooses scope beyond `factory/feature.md`" | `closed` 2026-08-25, **answered before the role ran** | **Botanical**, by the owner, approved 2026-08-25 from a drawn mockup of all five screens. A green page `#14513A`, never white, with warm yellow `#FFC94A` as the single action colour. A serif for the verdict, a grotesque for everything else, neither of them Inter or Roboto. **It took two attempts.** The first was warm paper `#F7F4EC` with a terracotta accent, and the owner rejected it in one line: that is Claude's own palette, reproduced by the assistant without noticing. Research then showed every app in `03-market.md` is green-on-white, so the fix was to put the green on the page rather than on a white page. **Contrast was measured against both surfaces at every step, and three proposed values were caught and replaced** — `#8A8578` at 3.35:1 in the first attempt, `#A9C7B5` at 3.9:1 on the raised surface in the second, and two published palettes whose own mid-green was too light to carry light text. Botanical was then **held against five researched alternatives on the identical screen** — Jade Workshop, Garden Party, Matcha, Deep Seaweed, Emerald Luxe — and kept. Garden Party was the real test, because it is the same idea with a green accent and no warm colour at all; it lost because the action button then competes with the verdict for attention | `factory/feature.md`, "The visual identity". Read next by 300-design, which turns it into `03-tokens.md`, then 500-engineering and 600-qa | Nothing. The gate was answered before the role was dispatched, which is the first time on this run that has happened |
| 20 | 300-design | **The two typefaces.** `factory/feature.md` says a serif for the verdict and a grotesque for the rest, rules out Inter and Roboto, and then says "300 Design names the two families and says why. The owner may replace either." A typeface is part of the visual identity, which this role's contract hands back to a person. The role was told to name them, so it named them and recorded the gate | `closed` 2026-08-25 | **Confirmed by the owner the same day, from a side-by-side of the real screen at the real sizes.** `Fraunces` for `--font-display`, `IBM Plex Sans` for `--font-body`. **EB Garamond is the named loser** — it was the face in the approved mockup and lost the direct comparison, so it is written into `factory/feature.md` as rejected rather than merely unused. The owner also checked `ő` and `ű` at 44px and found no problem. Fraunces appears on published lists of overused faces in generated design; the owner was told that before choosing and chose it anyway, so this is settled and not to be re-opened. Originally proposed by 300-design as: `Fraunces` for `--font-display` and `IBM Plex Sans` for `--font-body`.** Fraunces is a serif with real character and a wide weight range, which a 44 px verdict needs. IBM Plex Sans is a plain grotesque that is neither Inter nor Roboto. **One check is owed before either ships:** both families must draw the Hungarian letters `ő` and `ű`, or half the product falls back to another font. That check is written into `03-tokens.md` §2 | `docs/300-design/03-tokens.md` §2. Read next by 500-engineering, which loads the fonts, and 600-qa, which tests the two letters | The line continued with those two names. Replacing either changes two token values and nothing else, because no component names a font family directly |
| 21 | 300-design | **No story defines the screen that creates a pot**, and US-01 AC-3 tells the app to ask the user to pick or create one. A new user therefore reaches a dead end on the first screen of the feature. Designing that screen would be choosing scope beyond `factory/feature.md`, which lists only capturing or choosing a photo of an already named pot. Policy match: "a subagent chooses scope beyond `factory/feature.md`" | `closed` 2026-08-25 | **Add a pot: a name and a room, two fields, one screen**, by the owner, the same day. The smallest version only — species, photo and notes belong to backbone feature 4 and stay out. Written into `factory/feature.md` under In scope. **200 Product's story list is now one story short**, and closing this gate does not close that gap: a story has to exist before 500 Engineering can build the screen. Seeding pots straight into the database was rejected, because then the app cannot be handed to anyone and QA cannot test from a clean start. Originally recorded as: The empty state of `PotPicker` shows one sentence and a link out to a screen that this run does not specify, and the send button stays disabled with its reason. That satisfies US-01 AC-3 without inventing a feature | `docs/300-design/001-photo-assessment/02-SPEC.md` §3.3 and SC-1 state 2, and `01-CONTEXT.md` §4. Read next by 400-architecture and 500-engineering, which will find the link has no target | **The flow is not walkable end to end for a person with no pots.** The owner decides whether creating a pot joins run 1 or waits for run 2 with the schedule engine |
| 22 | 300-design | **US-02 and US-05 disagree about a malformed model answer.** US-02's AI Eval Card says a missing field, an unknown verdict code or an unknown band is treated as `cannot-tell`. US-05 AC-2 says every `cannot-tell` shows one reason from a closed list of four, and none of the four describes an unreadable answer. Adding a fifth reason **code** would change a closed contract that 200 Product owns. Policy match: "a subagent chooses scope beyond `factory/feature.md`" | `closed` 2026-08-25 | **An answer the app cannot read is a failure, not a verdict**, by the owner, who asked for the practice rather than a preference. Checked against Anthropic's own guidance, and the guidance changed the shape of the answer: make it nearly impossible first with **structured output** (`output_config.format`, or a tool with `strict: true` and `additionalProperties: false`), then **check `stop_reason` before reading the content** — `refusal` is a decline and must never be retried, `max_tokens` is a truncation that retrying repeats, and only a real schema failure earns the one retry. Never match on the raw text of the answer; parse it. No fifth `cannot-tell` reason is added, because all four of them mean *I looked at your photo and could not tell*, and a broken answer means nothing was looked at. One screen for the user, three separate entries in the log. Originally recorded as: SC-5 gains a state `unreadable-answer` where the reason line is replaced by one plain sentence saying the answer could not be read. The list of four reason codes is untouched, so no contract changes. The screen behaves as `cannot-tell` in every other way: no verdict, no task, another photo offered | `docs/300-design/001-photo-assessment/02-SPEC.md` §3.14 and SC-5 state 2, and `01-CONTEXT.md` D-3. Read next by 500-engineering, which writes the contract, and 600-qa | A screen message, not a fifth code. If 200 Product would rather add a fifth reason code, this state disappears and one AC changes |
| 23 | 300-design | **The `unsure` label colour fails normal-text contrast on the surface the owner put it on.** `factory/feature.md` measures `--color-warn` at 3.3:1 on `--color-raised`, and the band table puts that colour on that surface. Normal text needs 4.5:1; large text needs 3:1. Changing a colour the owner approved is a visual identity call, which this role hands back | `closed` 2026-08-25 | **Fixed in the colour, not in a size rule**, by the owner. `--color-warn` becomes **`#FFC2B0`**, measuring 6.00:1 on the ground and 4.60:1 on the raised band — AA at any text size, so no rule is left to forget. Its hue is 14° against the yellow accent's 42°, checked so the two do not read as two warm blurs. 300-design's size fix was correct and is now unnecessary. The general lesson is written into `factory/feature.md`: **a contrast number belongs to a pair of colours and a text size**, and the original fence wrote down only two of the three. Originally recorded as: The `unsure` label is only ever set at `--text-heading` (28 px) or larger, where 3:1 is the bar and 3.3:1 passes. Every smaller text inside the band uses `--color-body` or `--color-muted`, which both pass on that surface. **No approved colour was changed** | `docs/300-design/03-tokens.md` §1.2, `02-SPEC.md` §3.12, and `01-CONTEXT.md` D-1. Read next by 500-engineering and 600-qa, which tests the size rule | The band label is large text. If the owner wants a body-size warn label, the colour has to change and this row re-opens |
| 24 | 300-design | **No user interface component library is named in any input.** Choosing one is accepting a dependency, which `CLAUDE.md` lists as never the model's call. 400 Architecture and 500 Engineering have not run yet, so nothing exists to name. Policy match: "a subagent proposes a new tool" / accepting a new dependency | `closed` 2026-08-26 | **shadcn/ui, which uses Base UI underneath**, by the owner. shadcn is not a dependency — it copies a finished, styled component file into `apps/web` and that file is then edited. Base UI is the real dependency and supplies the behaviour. **The owner's requirement decided it:** hand-written CSS kept to a minimum, and Base UI on its own ships no styles, so nineteen components would each need their Tailwind written by hand. Rejected: React Aria on weight, Radix because 95 of its last 100 commits came from one person, shadcn on Radix for the same reason, and writing everything by hand. **The edit pass in ADR-0011 is now a condition of the decision, not a suggestion** — shadcn's defaults miss the 44 px targets, the 3:1 focus ring and the reduced-motion guard. Originally recorded as: **Not decided. 300-design named no library and proposed none.** Every component in `02-SPEC.md` §3 is written from nothing with its own state list. **Updated 2026-08-25 by 400-architecture: `docs/ADR/0011-build-the-components-on-base-ui.md` proposes Base UI, with the reasoning, the rejected options and the trigger to re-open in favour of React Aria.** That record is marked **Proposed, not Accepted**, because only the owner accepts a dependency. Its own summary says: do not add any component library to `package.json` until the owner has accepted it in writing | `docs/300-design/001-photo-assessment/02-SPEC.md` §1 and §3, `01-CONTEXT.md` D-6, and now `docs/ADR/0011-build-the-components-on-base-ui.md`. Read next by 500-engineering | Nineteen new components. **400-architecture counted them and found only one, `ConfirmSheet`, actually needs a primitive library** — that count is the fact the proposal turns on, and it is in the ADR |
| 25 | 300-design | Brand voice and the exact words on screen · any accessibility call needing a disabled person's experience · whether AI belongs in this feature · spending money · deploying · accepting a risk · reversing an ADR | `n/a` | The role wrote copy **rules** and no copy. `02-SPEC.md` §10 says what a sentence must do; it writes no sentence. Every accessibility line in §8 is a checklist item with a number, and none of them is a judgement about lived experience. No spend, no deploy, no risk accepted, no ADR touched | — | — |
| 26 | 400-architecture | **Two whole-system options scored one point apart.** `00-options.md` gives Option A (serverless, key-value, the call inside the request) 16 and Option D (no separate API, Next.js does everything) 15. This role's own contract names that as a stop: "two options score within one point and the choice is not defensible". Policy match: "a subagent chooses scope beyond `factory/feature.md`" — Option D would drop Nest.js, which the owner named in "Human decisions already made" | `closed` 2026-08-26 | **Option D is rejected by the owner. Option A stands.** *"option d that we use next.js only is not an option."* There will be a separate API, so Nest.js stays and the border between the screens and the work stays with it. No role may re-open this. Originally recorded as: **Not decided. Option A was taken and the gap was recorded.** The one point between them is a single trade: **D gains a point on C-3**, because one application really is simpler for one person to run than two, and **loses two on C-4**, how hard it is to leave. D is the one option that cannot obey five of the six split-readiness rules in `factory/feature.md`, because it has no border between the screens and the work for a later split to follow. Taking D would need the owner either way, since it removes a named stack decision | **`factory/feature.md`, "Human decisions already made"** — that is the file every role reads. Also `docs/400-architecture/00-options.md` §5 and §6. Read next by 500-engineering and 800-infra, both of which build against Option A | **Option A**, and the answer matched. Nothing written on this run has to change. Every ADR, both diagrams, the timed flow and the NFRs already assume it |
| 27 | 400-architecture | **The 180-day photo promise is printed on a screen and cannot be measured at 180 days.** US-10 AC-1 puts it in front of the person and M-07 asks for zero photos older than 180 days. AWS states: *"There may be a delay between the expiration date and the date at which Amazon S3 removes an object."* So a test demanding zero at day 181 fails on a working system. Policy match: "what counts as personal data, and how long it is kept" is a standing owner decision | `closed` 2026-08-26 | **The wording on the screen stays exactly as it is**, by the owner. It keeps saying photos are deleted after 180 days, and NFR-41 checks at 182. The reasoning the owner gave weight to: at day 180 the photo cannot be reached through the app and storage is no longer charged, which is what a person means by "deleted". "About 180 days" was rejected for making a clear promise vague. Describing only what is guaranteed — "no longer shown" — was rejected for sounding weaker than the truth. Originally recorded as: **Not decided. The measurement window was widened and the owner is told.** NFR-41 measures at **182 days**, not 180. Three things are true together and belong in one sentence: the photo is **not readable through the app** after 180 days, because the app only signs a URL for a row that still points at it; storage is **not charged** after expiry, per the same AWS page; and the object may physically exist a little longer. **The owner decides whether the screen's wording needs to change.** This role changed no wording and no retention period | `docs/400-architecture/06-nfrs.md` NFR-41, and `docs/ADR/0007-keep-photos-in-one-private-bucket-with-a-lifecycle-rule.md`. Read next by 800-infra, which writes the rule, and 900-security | 180 days as the rule, 182 days as the check. Nothing else changed |
| 28 | 400-architecture | **No story says what a stored next-action text shows after the reader switches language.** Every other value that crosses the wire is a code and renders in the reader's language. `nextAction` is prose the model wrote, once, in one language. US-11 AC-1 says every text in the flow is in the language being read. Policy match: "a subagent chooses scope beyond `factory/feature.md`" — translating an old assessment, or re-asking the model for it, would both be new work | `closed` 2026-08-26 | **Show the text as written, and name its language**, by the owner. A short caption line appears under the next action when the stored language is not the language being read, and never when they match. **New criterion US-11 AC-6**, and a new `WrittenInLine` in `02-SPEC.md` §3.11. Translating on read was rejected because it is a second paid model call on every read of an old assessment. Saying nothing was rejected as cheap but confusing. Originally recorded as: **Not decided.** Run 1 stores the language the text was written in and shows the text exactly as written. Nothing translates it and nothing re-asks the model. The three real options for the owner: leave it (cheapest, and US-11 AC-1 is not fully true for an old assessment); translate on read (a second paid call per read); or show a small line saying which language it was written in (cheap and honest). **The earliest role that could have carried this is 200-product**, which wrote US-11 and US-02 without noticing that one of the four fields is not a code | `docs/400-architecture/05-patterns.md` §10, and seam ledger finding 25. Read next by 500-engineering, which writes the contract field, and 600-qa | The text is shown as written, in whichever language the model produced it |
| 29 | 400-architecture | **A promise made to the user on a screen has no owner in run 1.** US-10 AC-2 tells the person an account with no sign-in for 12 months is deleted, *"with a warning by email at 11"*, and `factory/feature.md` calls the warning "not optional here". **Nothing in run 1 builds any way to send email** — no story, no acceptance criterion, no container. Policy match: "a subagent chooses scope beyond `factory/feature.md`" — adding a mail service is a new outside dependency | `closed` 2026-08-26 | **It joins the notifications run, backbone 6**, by the owner. That run needs a delivery channel anyway, so the mail service arrives with it and the warning comes for free. Nothing is deleted for twelve months, so nothing breaks before then. **The promise stays on the screen and nothing in run 1 keeps it** — that is the one place in this product where those two are apart, and it is deliberate rather than missed. Building it in run 1 was rejected as a new outside dependency with a sending domain to verify, for a promise nothing can trigger for eleven months. Removing the sentence from the screen was rejected because the rule itself is not changing. Originally recorded as: **Not decided, and not built.** `01-context.mmd` and `02-containers.mmd` draw the mail service as an outside box with the words "not built in run 1" on the arrow, so the gap is visible rather than absent. It blocks nothing today. It is the kind of gap that stays invisible for eleven months and then deletes somebody's plant history. **The owner decides whether it joins run 1, run 2 or the run that builds notifications (backbone 6)** — which is the run that needs a delivery channel anyway | `docs/400-architecture/01-context.mmd`, `02-containers.mmd`, and seam ledger finding 26. Read next by 800-infra | Nothing sends mail. The 12-month deletion rule has no warning behind it yet |
| 30 | 400-architecture | **How the admin actually operates a product with no admin screen, and where the reconciliation credential lives.** `01-use-cases.md` UC-7 left "how an admin reaches these figures with no screen" open, and US-12 AC-2 asks the app's model-call count to match the provider's own record exactly. That record needs an **admin** API key from Anthropic, which is a different and more powerful credential than the one the app uses. Policy match: "a subagent asks for secrets or credentials" and "a subagent proposes a new tool the model may call" | `closed` 2026-08-26 | **No admin route ships in run 1 at all**, by the owner: *"just do it in other run, we dont deal with admin now… me as dev i can delete whatever i want."* The developer reads the usage figures straight from the DynamoDB table and flips the kill-switch by editing its row in the AWS website, both with AWS credentials. **The two account types and the permission check still ship**, because US-14 AC-3 is about the admin route somebody adds next year and the default has to already be *refuse* when they do. **US-13's criterion asking the log to record which account flipped the switch was removed on the same day**, because with no route the app never runs when the flip happens; AWS keeps its own record, and there is one person. **US-12 moves out of run 1 whole.** This reversed ADR-0009's route, reversed `05-patterns.md` §12, and changed one sentence in `factory/feature.md`. Originally recorded as: **Not decided. This role's proposal, for the owner to accept or overrule.** The admin signs in in a browser like anybody else, then calls `POST /api/admin/ai-enabled` and the figures route with `curl`, carrying the same session cookie — which means copying a cookie out of a browser, and that is awkward. **The Anthropic admin key does not go on the server.** The reconciliation is a script the owner runs locally against `/v1/organizations/usage_report/messages`. Editing the DynamoDB row from the AWS console was rejected on a criterion, not on taste: US-13 AC-5 needs the **zamphora account id** in the log, and a console edit records an AWS principal instead. **900 Security confirms or overrules the credential placement** | `docs/ADR/0009-hold-the-kill-switch-in-the-table-and-cache-it-for-30-seconds.md`, `docs/400-architecture/05-patterns.md` §12, and `06-nfrs.md` NFR-13. Read next by 900-security, then 800-infra | The switch and the figures are ordinary routes behind `@Roles('ADMIN')`. No second credential, no shared secret, and no admin key on the server |
| 31 | 400-architecture | **No availability target exists anywhere.** No input names an uptime figure, an error-rate ceiling or a recovery time, and none of the fifteen stories asks for one. This role's contract says an NFR with no test approach is a stop-and-ask. Policy match: "a subagent needs a policy, compliance, budget or release decision" | `closed` 2026-08-26 | **No availability target in run 1, written down as a position rather than left as a gap**, by the owner. `06-nfrs.md` §9 now carries the full reasoning: one user, no second copy of anything, nobody on call, a model credit balance topped up by hand, and an AWS account that closes on 2026-12-31 by design. Inventing a number was rejected because 800 Infra would then build alarms against a promise nobody can keep or test. Originally recorded as: **Not decided, and no number was invented.** `06-nfrs.md` §9 records the gap rather than filling it. The stated assumption: a personal app with one user, an unattended credit balance topped up by hand, and a cloud account that closes on 2026-12-31 by design has nothing meaningful to promise yet. **The moment the app is offered to a second person this needs a number** — and that is the same trigger that re-opens gate 5, the EU AI Act question. Both should be answered together | `docs/400-architecture/06-nfrs.md` §9. Read next by 800-infra, which sets alarms, and 600-qa | No availability target. Nothing downstream was built against one |
| 32 | 400-architecture | **The sign-in method was never named, and naming it decides what a person sees.** `factory/feature.md` gives this role the protocol, the flow and where tokens live, and says nothing about email and password, a magic link or a social provider. Choosing Cognito's own sign-in pages also means the Botanical identity does not fully reach them, and how much Hungarian they show depends on Cognito rather than on our message files. Policy match: "a subagent chooses scope beyond `factory/feature.md`" | `closed` 2026-08-26 | **Email and password on Cognito's own hosted pages**, confirmed by the owner, who accepted that the sign-in page will not match the Botanical identity and that how much Hungarian it shows is Cognito's decision. Adding "Sign in with Google" was offered and not taken — it needs a Google Cloud project and a client secret to keep. Building our own sign-in screen was rejected for the same reason our own passwords were: it puts the password inside this application. Originally recorded as: **Not decided. The line continued on email and password through Cognito's own pages**, because it needs no extra vendor and no extra cost, and `01-CONTEXT.md` §4 already puts the sign-in screen out of scope for run 1. **The owner should know that "out of scope" now means "Cognito's pages", not "ours, later".** The alternative is building our own sign-in screen against Cognito's API, which puts the password in our application and is a real piece of work. The protocol and the token rules in ADR-0003 do not change either way | `docs/ADR/0003-sign-in-with-openid-connect-and-keep-tokens-on-the-server.md`. Read next by 300-design if a sign-in screen is ever designed, then 500-engineering and 900-security | Cognito's hosted pages, email and password. No screen in this repository |

| 33 | 500-engineering | **Nothing names a unit test runner.** `06-nfrs.md` names Playwright for end-to-end, `size-limit` for bundle size and CDK assertions for infra, and nothing runs the `test` job that most NFRs point at. Policy match: accepting a new dependency, which `CLAUDE.md` lists as never the model's | `recorded-open` 2026-08-26 | **Not decided, and nothing was installed.** The specs name the enforcement point as `test` without naming what runs it. `.claude/skills/testing-patterns/SKILL.md` in this repo already describes Vitest conventions, so the repo has an opinion the specs do not carry. **The owner accepts the dependency or names another** | `docs/500-engineering/00-conventions.md`. Read next by 600-qa, which writes the test plan against whatever this is | The specs, with the runner named as a hole rather than filled in |
| 34 | 500-engineering | **Parsing the uploaded photo needs a library nobody has accepted.** `POST /api/assessments` takes `multipart/form-data` (ADR-0007), and Nest's file interceptor pulls in multer. Policy match: accepting a new dependency | `recorded-open` 2026-08-26 | **Not decided.** The route is specified and the parser is not. This one is easy to miss because it arrives as a side effect of a framework feature rather than as a line somebody adds to `package.json`, which is exactly why it is written down | `docs/500-engineering/03-api-spec.md`. Read next by 900-security, which should look at any upload parser | The upload route, with the parser unnamed |
| 35 | 500-engineering | **Is `room` required on a pot, and how long may it be?** US-15 AC-2 asks for two fields and AC-4 gives a length rule for the name only. Policy match: what gets built — a required field is a product decision | `recorded-open` 2026-08-26 | **Assumed, and marked as assumed.** The contract says required, 1 to 60 characters. The role did not leave the field out and did not pretend the rule was given. **The owner confirms or changes it**, and it is one line in `01-contracts.md` either way | `docs/500-engineering/01-contracts.md`. Read next by 600-qa | A required `room`, 1 to 60 characters, flagged |
| 36 | 500-engineering | **The care task date uses the UTC calendar day, and nothing said it should.** No input names a timezone for a care task. Policy match: what gets built | `recorded-open` 2026-08-26 | **Chosen with the reason written down.** UTC was picked so the product has exactly one definition of "day" — the same one the daily quota uses — and so nothing depends on a timezone the browser sends. **The cost is real and the owner should see it:** a Hungarian reader sees the day roll over at 01:00 or 02:00 local time, not at midnight | `docs/500-engineering/01-contracts.md` and `03-api-spec.md`. Read next by 600-qa | UTC days, everywhere, consistently |
| 37 | 500-engineering | **US-15 has an endpoint and a route and no design.** `02-SPEC.md` names "the create-a-pot screen" in SC-1 state 2 and in `PotPicker` `empty`, and never specifies it. This is gate 21 reappearing one role later: the owner said the screen joins run 1, and 300 Design had already finished | `recorded-open` 2026-08-26 | **Not decided. The role built the minimum from the acceptance criteria and said so.** `02-web-spec.md` §9 defines the route `/[locale]/pots/new` and the two fields. **The state list, the layout and the copy are 300 Design's and do not exist.** The owner decides whether 300 Design runs again for one screen, or whether the minimum ships as is | `docs/500-engineering/02-web-spec.md` §9, and seam finding 31. Read next by 600-qa, which cannot write screen tests without the states | A route and two fields, with no visual spec behind them |

### Three of 200's gates are gates this run already has

200-product did not know that, because it reads no run bookkeeping. Recorded here so they are
answered once, not twice.

- **Its G-3, how long the assessment text is kept, is gate 10 above.** 200 raises it from
  `recorded-open` to a **hard stop**, because US-10 puts the period on a screen and there is no
  period to print. Gate 10 said it becomes a hard stop at 400 Architecture. It became one earlier.
- **Its G-4, the 8-in-10 acceptance target, is gate 3 above,** which the owner closed on 2026-08-24
  by keeping the number and marking it provisional. 200 carried it through as M-01 with the same
  label. Nothing more is owed here.
- **Its G-10, whether the EU AI Act applies, is gate 5 above,** still open. 200 took the same cheap
  safe assumption 100 did: US-06 shows the AI notice whether or not the Act applies.

## Gates that should have fired and did not

This section is the valuable one. Read each slot's output and ask: did it decide something that was
not its to decide?

1. **100-consulting — `missed`, found by the owner reading the brief.**
   `00-context-brief.md` line 20 says watering, soil and placement are "a schedule problem, and a
   calendar solves those". Those are backbone features 1, 2, 3 and 6, already approved by the owner.
   Ranking an approved feature down is a "what gets built" decision, which is the last row of the
   standing list below. The brief contradicts itself at line 165. The line did not catch this; a
   person did, on a first read. `factory/feature.md` now says a calendar app is not the answer to
   any of the four, so 200 Product reads the correction from the input rather than from chat.

2. **100-consulting — otherwise checked, none found.** The four outputs propose numbers (the value
   hypothesis, the cost estimate, the assessments-per-month assumption) and every one of them is
   labelled in place as proposed or estimated, with the owner named as the person who confirms it.
   The one to re-check when 200 Product runs: does the brief's cost table read downstream as a model
   *choice*? It is not one. `02-decisions.md` D-10 says so out loud. If 400 Architecture treats
   Haiku 4.5 as already chosen, this becomes a `missed` gate and the fault is in the brief.

### Second pass, 2026-08-24 — checked again

3. **Gate 1 above is now repaired in the files, not only in the input.** The "a calendar solves
   those" sentence is gone from `00-context-brief.md` §1, and the same fault has been removed from
   two places in `01-use-cases.md` that the first check did not name: UC-1's Value gate said photo
   assessment was "the only one of his four problems that a calendar cannot solve", and UC-2's
   "Why AI beats it here" said "A phone calendar works." Both now quote `factory/feature.md`
   instead. The `missed` label on row 1 stands, because it did happen.

4. **No new missed gate found.** The second pass closed five open questions from the inputs and
   opened one (O-10, gate 10 above). It decided nothing itself. The two numbers it changed —
   "four plants" to "at least eight pots", and the reasoning under hypothesis V2 — are corrections
   of a fact from `initial-plan.md`, not new judgements. The targets themselves (8 in 10, 6
   assessments) were not touched, because gate 3 records the owner keeping them.

### 200-product, 2026-08-24

5. **200-product — `missed`. It set the build order for the whole product.** `00-prd.md` §3 gives
   every backbone feature a run number: watering and soil in "Run 2 (planned)", notifications in
   "Run 3", placement in "Run 4", documenting over time in "Run 5". `factory/feature.md` lists the
   six backbone features and **never assigns a run to any of them**. So those five numbers were
   invented here. 200's own slot contract says it must hand back "what gets built first", and
   `00-prd.md` §8 says out loud that build order is never decided in that document. The table
   contradicts the rule three sections later in the same file.

   It is a small mistake with a long reach: 300 Design and 400 Architecture read the PRD and will
   treat "run 2" as settled. **Read that column as "not this run" and nothing more.** The owner
   decides what run 2 is. Nothing else in the PRD depends on the numbers — every story in this run
   serves backbone feature 5, and §3 says so.

6. **The re-check from row 2 above is answered: no.** Row 2 asked whether the brief's cost table
   would read downstream as a model *choice*. It did not. `00-prd.md` §10 lists "which model is
   used" as a thing this document does not decide, and no story names a model. The brief's warning
   in D-10 held for one hand-off. Ask again after 400 Architecture, which is the role that picks.

### 300-design, 2026-08-25

7. **The warning in row 5 held.** 300-design read `00-prd.md` §3 with its five invented run numbers
   and did not treat any of them as settled. `01-CONTEXT.md` §6 point 8 says out loud that "run 2"
   and "run 4" are read as "not this run" and nothing more, and every Out row in §4 names the owner
   as the person who decides when a thing comes back. **Ask the same question again after 400
   Architecture**, which is the next role to read that table.

8. **300-design — one thing to watch, not a `missed` gate.** Decision D-4 in `01-CONTEXT.md` adds a
   line that no acceptance criterion asked for: the number of assessments left today, shown only
   when 2 or fewer remain. It is a design call inside US-08 AC-2 and AC-4, it breaks no criterion,
   and the role labelled it as removable in one line. It is written here because an unasked-for
   element is exactly the shape a scope decision hides in. **The owner can strike it and nothing
   else changes.**

9. **300-design — otherwise checked, none found.** The role hit five stop-and-ask conditions
   (gates 20 to 24), recorded all five, and continued on a stated assumption for each. It named no
   library, wrote no sentence of product copy, changed no approved colour, and added no verdict
   code, band or reason code.

### 400-architecture, 2026-08-25

10. **The re-check from row 2 and row 6 is answered, and the answer is no.** Row 2 said: *"If 400
    Architecture treats Haiku 4.5 as already chosen, this becomes a `missed` gate and the fault is
    in the brief."* It did not.
    `docs/ADR/0006-choose-the-model-by-measuring-it.md` turns the choice into a measurement — run the
    40-photo golden set on all three for $1.28, take the cheapest that meets the 8-in-10 agreement
    bar — and names "pick Haiku 4.5 outright, on price" as a rejected option, with row 2 quoted as
    the reason it is rejected. Haiku is the run-1 default only *until* the measurement exists, and
    the record says so in those words. **The warning in the brief did its job across two hand-offs.**

11. **The warning in row 5 held a second time.** 400-architecture read `00-prd.md` §3 with its five
    invented run numbers and treated none as settled. Where a run number appears in an ADR it is
    only ever inside a **trigger** — "re-open this when run 2 adds a date picker", "the first index
    arrives when a run needs a query across accounts" — which is a condition, not a schedule. No ADR
    says when anything ships.

12. **400-architecture — one thing to watch, not a `missed` gate.** Two numbers in ADR-0007 were
    chosen by this role and appear in no input: a **2 MB** ceiling on the request body, and a
    **5-minute** life on a signed photo URL. Both sit inside numbers the owner did set — the 1000 px
    resize rule and the general rule that a photo is private — and both can be struck in one line
    without breaking an acceptance criterion. They are in the same category as 300 Design's D-4.
    Two smaller ones, listed for the same reason: the **30-second** cache lifetime in ADR-0009 (the
    owner's promise is 60), and the **UTC calendar day** as the reset boundary in ADR-0008, where no
    input names a timezone.

13. **400-architecture — otherwise checked. Seven stop-and-ask conditions, all recorded.** Gates 26
    to 32. It named no scope beyond `factory/feature.md`, proposed no spend, deployed nothing,
    accepted no risk, reversed no ADR, and **did not accept the component library it proposed** —
    ADR-0011 is marked Proposed and its own summary forbids adding the package until the owner says
    yes. It also did not write `07-adversarial.md`, which by its contract must come from a session
    that never saw its reasoning.

    **The one to re-check after 500 Engineering:** ADR-0011 proposes Base UI, and three files in
    `.claude/skills/` name Radix and say "Radix does the hard parts". No role reads `.claude/skills/`.
    If 500 Engineering writes conventions against Radix without noticing, that is the exact break
    finding 21 predicted, and it is a `missed` gate at that point rather than this one.

## Standing human-owned decisions on this project

Recorded here so a `missed` is easy to spot. None of these is ever a subagent's call.

- What gets built, and what ships when.
- Accepting any risk, security or architectural.
- What counts as personal data, and how long it is kept.
- Spending money. Deploying anything. Owning the kill-switch.
- Accepting a new dependency, or a new tool the model may call.
- Reversing an accepted ADR.
- The final merge, and the release go/no-go.
